export const createEmail = (emailadress: string) => 'correo electornico'
export const sendEmail = (emailadress: string) => {
    const email = createEmail(emailadress)
    console.log('el correo fue enviado');
    
}